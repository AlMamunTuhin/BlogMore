<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile - Notes</title>
    <link rel="stylesheet" type="text/css" href="../user.css">
</head>
<body>
    <div class="container">
        
        <div class="user-profile">
            <p> <%= user.username %> </p>
        </div>
        
        <div class="notes-section">
            <h1 id="header">His Short Blogs </h1>
            <ul>
              <% blogs.forEach(blog => { %>
                <li class="notes">
                  <h3> <a method="GET" href="/blogs/<%= blog._id %>"><%= blog.title %> </a> </h3>
                </li>
              <% }) %>
            </ul>
          </div>

    </div>
</body>
</html>
