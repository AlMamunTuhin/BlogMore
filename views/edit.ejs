<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog - BlogMore</title>
</head>

<body>
    <div class="edit-container">
        <h1 class="edit-title">Edit Blog</h1>


        <form action="/blogs/<%= blog._id %>?_method=PUT" method="POST" class="edit-form">
   
            <div class="form-group">
                <label for="title" class="form-label">Blog Title</label>
                <input type="text" id="title" name="title" class="form-input" value="<%= blog.title %>" required>
            </div>

       
            <div class="form-group">
                <label for="content" class="form-label">Blog Content</label>
                <textarea id="content" name="content" rows="10" class="form-textarea" required><%= blog.content %></textarea>
            </div>

     
            <button type="submit" class="btn-submit">Update Blog</button>


            <a href="/blogs/<%= blog._id %>" class="btn-cancel">Cancel</a>
        </form>
    </div>
</body>

</html>
